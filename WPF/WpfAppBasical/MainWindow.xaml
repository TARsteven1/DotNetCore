<Window x:Class="WpfAppBasical.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppBasical"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Window.Resources>
		<Style x:Key="defaultBaseStyle" TargetType="Button">
			<Setter Property="FontSize" Value="40"/>
			<Setter Property="Foreground" Value="Blue"/>
		</Style>
		<Style x:Key="defaultFirstStyle" TargetType="Button" BasedOn="{StaticResource defaultBaseStyle}">
			<Setter Property="FontSize" Value="30"/>
		</Style>
		<Style x:Key="TriggerStyle" TargetType="Button" BasedOn="{StaticResource defaultFirstStyle}">
			<Setter Property="FontSize" Value="40"/>
			<!--Trigger : 监测依赖属性的变化、触发器生效-->
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="FontSize" Value="60"/>
					<Setter Property="Foreground" Value="Yellow"/>
				</Trigger>
				<Trigger Property="IsMouseOver" Value="False">
					<Setter Property="FontSize" Value="40"/>
					<Setter Property="Foreground" Value="Red"/>
				</Trigger>
			</Style.Triggers>
		</Style>
		<Style x:Key="MultiTriggerStyle" TargetType="Button" BasedOn="{StaticResource defaultBaseStyle}">
			<Style.Triggers>
				<MultiTrigger>
					<MultiTrigger.Conditions>
						<Condition Property="IsMouseOver" Value="true"/>
						<Condition Property="IsFocused" Value="true"/>
					</MultiTrigger.Conditions>
					<MultiTrigger.Setters>
						<Setter Property="Background" Value="Green"/>
					</MultiTrigger.Setters>
				</MultiTrigger>
			</Style.Triggers>
		</Style>
		<Style x:Key="EventTriggerStyle" TargetType="Button" BasedOn="{StaticResource defaultBaseStyle}">
			<Style.Triggers>
				<EventTrigger RoutedEvent="Mouse.MouseEnter">
					<EventTrigger.Actions>
						<BeginStoryboard>
							<Storyboard>
								<DoubleAnimation Duration="0:0:0.2" Storyboard.TargetProperty="FontSize" To="10"/>
							</Storyboard>
						</BeginStoryboard>
					</EventTrigger.Actions>
				</EventTrigger>
			</Style.Triggers>
		</Style>
	</Window.Resources>
	<StackPanel Background="LightBlue">
		<TabControl Height="420" Margin="0,0,-0.4,0" >
			<TabItem Header="布局">
				<!--Grid : 类似table表格、可灵活设置行列并放置控件元素、比较常用-->
				<Grid>
					<!--UniformGrid : 指定行和列的数量, 均分有限的容器空间-->
					<Grid.RowDefinitions>
						<RowDefinition Height="auto"/>
						<RowDefinition/>
					</Grid.RowDefinitions>

					<Grid.ColumnDefinitions>
						<ColumnDefinition/>
						<ColumnDefinition Width="2*"/>
					</Grid.ColumnDefinitions>
					<Border Background="Red">
						<!--StackPanel: 水平或垂直排列元素、Orientation属性分别: Horizontal / Vertical-->
						<StackPanel Orientation="Horizontal">
							<Button Width="100" Height="20"/>
							<Button Width="100" Height="20"/>
							<Button Width="100" Height="20" Content="111"/>
						</StackPanel>
					</Border>
					<Border Background="Green" Grid.Column="0" Grid.Row="1">
						<!--DockPanel : 根据容器的边界、元素进行Dock.Top、Left、Right、Bottom设置-->
						<DockPanel LastChildFill="False">
							<Button Width="100" Height="20" DockPanel.Dock="Left"/>
							<Button Width="100" Height="20" DockPanel.Dock="Top"/>
							<Button Width="100" Height="20" DockPanel.Dock="Right" Margin="0,171,0,190.6"/>
						</DockPanel>
					</Border>
					<Border Background="Blue" Grid.Column="1" Grid.Row="1">
						<!--WrapPanel : 水平或垂直排列元素、针对剩余空间不足会进行换行或换列进行排列-->
						<WrapPanel>
							<Button Width="100" Height="20"/>
							<Button Width="100" Height="20"/>
							<Button Width="100" Height="20"/>
						</WrapPanel>
					</Border>
				</Grid>
			</TabItem>
			<TabItem Header="样式&amp;触发器">
					<StackPanel>
					<Button Style="{StaticResource defaultFirstStyle}"  Content="hel" Width="100" Height="40"/>
					<Button Style="{StaticResource TriggerStyle}" Foreground="Red" Content="hel" Width="100" Height="40"/>
					<Button  Style="{StaticResource EventTriggerStyle}"    Foreground="Pink" Content="hel" Width="100" Height="40"/>
				</StackPanel>
			</TabItem>
		</TabControl>
	</StackPanel>
</Window>
